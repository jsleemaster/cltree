---
title: "Usage"
---

```bash
# Start in current directory
claude-explorer

# Start in specific directory
claude-explorer --path /path/to/project
```

When the Claude Code CLI changes its working directory, the file tree updates to show that location.
